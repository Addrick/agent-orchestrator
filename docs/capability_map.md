# Capability Map

**What this is for:** the other two docs are indexed by *module*. `architecture.md`
answers "what is `src/foo.py`?" and `user_guide.md` answers "what can I do from
Discord?". Neither can answer **"what already implements capability X?"** — so
when a capability gets built a second time, nothing in either doc looks wrong.
The new module simply gets a new section.

This file is the inverse index: capability → implementations. Its job is to make
a second implementation *visible* at the moment someone adds one.

**Read this before building anything that parks, schedules, stores, notifies,
retries, or spawns.** Search by intent, not by name — a re-derived implementation
never shares the name of the original (DP-302).

**Verdict column:**

- `single` — one owner. Reuse it.
- `by design` — several implementations, deliberately, with a recorded reason.
  Adding a third needs the same bar: a decision record, not a preference.
- `unreviewed` — several implementations, no recorded decision. Not automatically
  wrong; nobody has ruled. These are the audit queue.

Keep rows sorted by capability. When you add a capability, add a row. When you
add a *second* implementation of an existing capability, change the verdict to
`by design` and link the decision — or don't write it.

---

## Human-in-the-loop

| Capability | Implementations | Verdict |
|---|---|---|
| Park a write call for human approval | `confirmations.ConfirmationManager` (chat turns, keyed by `token`, **N pending per user+persona** since DP-297, **durable in SQLite `Parked_Writes` since DP-319**, each resolved by its own continuation turn) · `proposals` queue (agent writes, SQLite `Proposals`, `proposal_id`, approved by LLM tool, run by `ProposalExecutor`) | `by design` — ADR 2026-07-04. **DP-319 closed the last property gap (durability), so the split is now justified by the executor boundary alone, not by any missing capability.** `tools/mcp_bridge.py:200` correctly routes into the same proposal store rather than adding a third — via an **injected `propose_call` callable**, deliberately not importing the store (the bridge must not depend on `src.proposals` internals). DP-297 narrowed the gap to durability and DP-319 closed it; `Parked_Writes` is deliberately a **separate table from `Proposals`** so a row shaped like "call any tool" is never one executor bug away from the whitelist. Convergence was considered and rejected — `ProposalExecutor` dispatches a *whitelist* of `action_type`s (`executor.py:113-135`, plus the `call_derpr_tool` short-circuit at `:48`), and that whitelist IS the privilege separation, so hosting arbitrary chat tool calls would mean deleting it. Any future convergence needs its own decision record first. |
| Approve/deny a parked item | `POST /api/v1/persona/{name}/confirm` (portal/HTTP, `token` **required** since DP-297) · Discord `on_raw_reaction_add` → `ChatSystem.resolve_park` · `approve_proposal`/`deny_proposal` LLM tools (proposal queue) | `by design` — same split as above. The two chat surfaces are thin: both funnel into the single `stream_resolve_park` entry point, which owns the claim/lock/execute/patch/continue sequence. ⚠️ **RAW, not `on_reaction_add`** (DP-319 review): discord.py dispatches the cooked event only for messages still in its `max_messages` client cache, which is empty after a restart — so once parks became durable the cooked handler could neither answer a stale button nor resolve a live park whose message had aged out. Any future reaction-driven affordance over durable state has the same constraint. ⚠️ **The Discord re-post is channel-scoped in `_post_pending_proposals`, not in the store** — `list_for` is keyed `(user, persona)` and knows nothing about channels, so the surface has to filter on `ParkedWrite.channel` itself (matching the expression `on_message` passes to `generate_response`: guild channel *name*, or the literal `"DM"`). Without that filter a durable park raised in a DM is re-posted into whatever public channel the operator next speaks in, `confirmation_text` and all. Any second surface that renders the pending list owes the same filter. |
| Suppress a duplicate proposal | `tool_loop.write_call_identity` (tool name + canonicalized arguments, call id excluded) consulted via the `pending_lookup` closure `ChatSystem._orchestrate` injects | Only implementation. Introduced DP-297. Note the tool result's `instruction` text ("do not re-submit") is *not* a second implementation — it is a request to the model; this is the enforcement. Keyed on **pending**, not on history, so a decided action can legitimately be proposed again. **DP-319 added a second, narrower predicate** — `ConfirmationManager.already_resolved` via the `resolved_lookup` closure — because the pending one is structurally blind during a continuation turn (the park has already been `take`n) which is exactly when the model re-proposes. Not a duplicate implementation: it answers a different question (*did this already RUN?*), shares the same identity rule via `tool_loop.write_call_identity_hash` (the storage digest, deliberately kept beside `write_call_identity` rather than on `MemoryManager` — drift between canonicalizer and digest fails OPEN as a disabled guard), and is triple-scoped so it cannot swallow the legitimate repeat the pending guard deliberately allows — to parks that RAN (`approved` **and** `approved_but_failed`, since a tool that raised may still have taken effect — ⚠️ that arm is only reachable because `apply()` derives `Decision.ok` from the *content* of what `execute_tool` returned; `ToolManager.execute_tool` catches every handler exception and returns `{"error": ...}` rather than raising, so deriving `ok` from "the call returned" silently made `approved_but_failed` dead in production and recorded a failed irreversible write as `approved`; DP-323 moved that derivation to the shared `tool_manager.tool_error()` predicate, which **widens this arm** to the writes that fail by *returning* (every proxmox write, `dispatch_fix`, `approve_proposal`) — the safe direction, since a soft-failed reboot may equally have taken effect), to `PARK_REEXECUTION_GUARD_WINDOW`, and to **continuation turns only** (`ChatSystem._already_resolved` returns None when `continuation is None`). That last scope is load-bearing and asymmetric with the pending guard: suppression here emits no `WriteParkedEvent` and no `PendingConfirmationEvent`, i.e. no affordance on any surface, so on an ordinary turn it would answer a deliberate repeat with "that already happened" and no way through. The predicate is injected rather than imported because `ConfirmationManager` sits above `src.tools` in the layer order. |
| Serialize concurrent work on one conversation | `ConfirmationManager.lock_for((user, persona))` — one `asyncio.Lock` per conversation, held across execute → patch → continuation | Only implementation. Introduced DP-297: with N proposals resolvable at once, two approvals would otherwise run two tool loops over the same history, each writing its own assistant row. Guarantees serialization; folding several decisions into one summary is best-effort (an uncontended lock does not suspend, so the winner is usually already inside its continuation). |
| Decide whether a call needs approval | `definitions.is_write_tool()` — tool-loop universal write audit (registry `is_write`; the import-time `WRITE_TOOLS` set is **gone**, DP-268) · `mcp_bridge._is_gated` (`is_write` OR `capabilities.irreversible`) · `definitions.is_irreversible()` + `tools/classifiers.py` argument-aware reversibility · `definitions.ALWAYS_CONFIRM_TOOLS` (name-keyed strictest tier, defined `definitions.py:174`, consumed by `tool_loop.py:511`) | `by design` — **composition, not one list**, resolved DP-306. Four predicates asking genuinely different questions ("is it a write" / "does it cross the agent trust boundary" / "is it irreversible *for these arguments*" / "is it confirm-always"), but all now derive from one declaration source: the tool definition's `is_write` + `capabilities` block. `validate_tool_capabilities` rejects a callable tool that omits `is_write`, so an unclassified tool **cannot register** — a stronger gate than a test. Non-`function` entries (engine signals like `google_grounding`) are explicitly exempt. Differential audit over all 45 tools found **zero disagreement**; `tests/tools/test_tool_definitions.py` pins the predicates to each other and fails if they drift. |
| Ask a running subagent a question | `self_edit` `answer_agent` tool + `AgentRecord.status == waiting` | `single` |
| Decide whether a tool call **failed** | `tool_manager.tool_error()` — consumed by `tool_loop._execute_calls` (colours `ToolCallResultEvent.error` / the portal ToolCard) and by `ConfirmationManager.apply` (derives `Decision.ok` → `approved_but_failed` in durable history + `executed_ok` in the audit row) | `single` since DP-323. Was two re-derivations of `result.get("error")`, one per consumer, and **both were wrong the same way**: `execute_tool` wraps a normal return as `{"result": <payload>}`, so the seven gated writes that report failure by *returning* (`{"status": "error"}` — every proxmox write + `dispatch_fix`; `{"executed": False}` — `approve_proposal`, which catches its executor's exception by design and therefore *cannot* raise) sat one level below where both looked. On the tool-loop side that mis-coloured a card; on the write-gate side a failed irreversible write was recorded as `approved` with `executed_ok=True`. Deliberately narrow — over-reporting is the dangerous direction, because a successful irreversible write marked failed invites the model to re-propose it. **A new handler must raise or use one of the recognized markers; a third failure idiom will be read as success.** |

## Authorization

| Capability | Implementations | Verdict |
|---|---|---|
| Match an origin against an allowlist | `origin.parse_operator_allowlist` + `origin.is_discord_operator` (`server_id[/channel_id[/author_id]]`, `*` wildcards, malformed entries dropped so a typo narrows and never widens) | `single` — one parser and one matcher, shared by both gates below. DP-330 deliberately reused it rather than writing a second matcher: a persona allowlist and the operator allowlist are the same *shape* of question, so the entry syntax an operator learns once holds everywhere. `origin.is_origin_allowed` is a thin wrapper (empty = unrestricted, non-Discord transports refused), not a second matcher. |
| Decide whether an origin may run a control-plane command | `message_handler.BotLogic.preprocess_message` — `CONTROL_PLANE_COMMANDS` gate on `Origin.operator` (DP-277) | `single` — deny-by-default at dispatch for the whole command, not a per-field list (that list rots). |
| Decide whether an origin may address a persona at all | `Persona.is_addressable_from` (`origin_allowlist` field) enforced once in `BotLogic.preprocess_message`, above both command parsing and the DP-277 control-plane gate | `single` — asks a different question from the row above (*may you reach this persona* vs *may this command reconfigure it*), which is why it is a separate gate rather than a widened `Origin.operator`. ⚠️ **Placement is the whole finding.** The first cut enforced it in `ChatSystem._orchestrate`; `discord_bot.on_message` and the portal's `dev_command` route both resolve dev commands through `preprocess_message` and **return without entering the kernel**, so the gate did nothing on the only transport it can match. It belongs at the shared chokepoint — three message-bearing surfaces, one seam — or every adapter that short-circuits a command re-opens the hole silently. Empty allowlist = unrestricted, so it is inert for every persona that does not opt in; a wholly-malformed list fails closed (unreachable) rather than parsing down to empty. Skipped on continuation turns because the kernel skips preprocessing there — structural, not a special case. **Does not cover the portal's read seams** (`GET /api/v1/persona/{n}`, `/assemble`, `/transcript`, `/kobold_export` bypass `preprocess_message` and the GET-exempt control-plane auth); that is a portal-auth gap, tracked separately. |

## Scheduling and deferred work

| Capability | Implementations | Verdict |
|---|---|---|
| Run work on a repeating interval | `agents/base.Agent._wait_for_next_run` (`schedule.interval` or `daily_at`, respects `_shutdown_event`) · `MemoryConsolidator.start_daemon` (`_shutdown_event` + `stop()`, drains between targets/clusters) · `mcp_client._maintenance_loop` (`MCP_RECONNECT_INTERVAL`, own wake/rate-limit logic; cancelled by its owner's `aclose()`) · `app_manager.register_task` (hosts the above; `stop=` for drainers, immediate cancel for the rest) | `by design` — decision `2026-07-30-interval-loop-shutdown-contracts` (DP-304). Two contracts, chosen by who owns the loop: **drain-based** for work-item loops hosted by `AppManager`, **cancel-based** for maintenance loops owned and closed by their component. DP-304 fixed the real defect (consolidator had no stop condition at all, and `AppManager.shutdown()` ignored `_running_tasks`). Verified 2026-07-30. |
| Signal a task, drain it with a bounded wait, then cancel | `AppManager.shutdown()` (every registered task; per-task budget via `register_task(drain_timeout=)`, default `GRACEFUL_SHUTDOWN_SECONDS`; tasks with no `stop=` are cancelled immediately, never drained; cancellation itself bounded by `CANCEL_GRACE_SECONDS`) · `AgentManager.stop_agent` (one agent; `instance.stop()` → `wait_for(task, 30.0)` → `cancel()`, hardcoded, cancel not awaited) · `MCPClientManager.aclose()` (cancel-first, no signal phase; `CLOSE_TIMEOUT_SECONDS` bounds both the maintenance task and the per-server `conn.stop()`) | `by design` — decision `2026-07-30-interval-loop-shutdown-contracts` (DP-304). The three differ by *scope*, not by policy: whole-process, one agent, one subsystem. **Residual:** `stop_agent`'s 30s is a literal that `GRACEFUL_SHUTDOWN_SECONDS` does not reach, and it cancels without awaiting the unwind — the two bounds should converge before a fourth caller appears. |
| Fire once at a future time | `voice/timer.TimerService` (in-memory) · Zammad `pending reminder` state via `ProposalExecutor` · `ReminderAgent` polling | `unreviewed` |
| Expire/GC stale state | `expire_stale_proposals()` (lazy sweep on read) · `ConfirmationManager.sweep_expired` (lazy sweep on read) + `rebuild_from_store` boot pass + `purge_parked_writes` retention, driven off the same sweep and throttled by `PARK_PURGE_INTERVAL` (DP-319) · `prune_agents` (fixr reaper) · LRU caps (`MAX_CONVERSATION_TAINTS`, `MAX_CACHED_API_REQUESTS`) · Hindsight 24h doc-scope gap | `unreviewed` — see [[DP-308]]. DP-319 deliberately reused the lazy-sweep-on-read idiom rather than adding a background loop (a periodic task here would re-open the DP-304 shutdown-contract problem), but had to add a **boot pass** on top: a lazy sweep only walks what is loaded, so a durable row the process never loaded is one it never expires. Any future consolidation must keep that pairing — sweep-on-read alone is not sufficient once the state outlives the process. The converse also holds and cost a review finding: a **boot pass alone is not sufficient either**, because the process durability exists for is the one that never reboots, so the retention purge had to hang off the read sweep as well. **Third pairing, same shape:** a retention purge that only deletes *terminal* rows means any row the boot pass declines to finalize — unreadable payload, or anything raising during reconciliation — is never expired and never collected. Both branches force it to `quarantined` for that reason, not merely to stop the log noise. |

## State and storage

| Capability | Implementations | Verdict |
|---|---|---|
| Persist structured state | `memory_manager` main SQLite (`MEMORY_DATABASE_FILE`) · `self_edit/store.AgentStore` (`CC_FIXR_REGISTRY_DB`) · `_DocScopeStore` (`HINDSIGHT_DOC_SCOPE_DB`) · `_TrustOverrideStore` (`HINDSIGHT_OVERRIDE_DB`) · `data/mcp_servers.json` (`MCP_SERVERS_FILE`) · `data/personas.json` (`PERSONA_SAVE_FILE`) | `by design` — DP-303 (2026-07-30). Six stores, four SQLite, and that is fine: each is a self-contained subsystem (fixr is meant to be lifted out whole; the Hindsight stores are local shims over an external service; the main DB is the raw-transcript backstop every future memory system reads from). The rule is not one store — it is **a store may own its own file, but not its own path convention**: every path resolves through `global_config` and defaults under `DATA_DIR`, the only mounted volume. Enforced by `tests/integration/test_store_path_policy.py`. |
| Read/write a row with the shared connection | `MemoryManager._get_connection`/`_lock` · `SqliteSemanticBackend` reaching back through `mm._lock` | `by design` — DP-108 layer split; the backend deliberately shares one connection so transactions span both layers. (Note: `arch_audit similar` scores 5 method pairs across these two at 0.57-0.63. That is the *cost* of the design, not a defect.) |
| Record an audit trail | `memory_manager.log_audit_event` · `Agent_Actions` trajectory rows (`_log_task_root`/`_log_step`) · proposal review columns | `unreviewed` |
| Redact secrets before they leave the process | `scrubber.get_scrubber().scrub()` called at four sinks: tool results → history + `ToolCallResultEvent` (`tool_loop._execute_calls`) · `audit_info` at the write gate (`tool_loop.run`) · cached api_payload (`turn_persistence.store_api_request`) · `Audit_Log` rows (`memory_manager._log_audit_event`) | `by design` — one scrubber, four call sites, because there is no single chokepoint every egress path crosses. Sites are chosen so each redacts as late as possible while still preceding the sink, and callers may legitimately hold the raw value (an approved write must execute with its real arguments). The audit sink is the only **permanent** one, which is why it scrubs at the sink rather than trusting its nine callers. Pinned by `tests/security/test_egress_enforcement.py` (boundaries 1-4). |

## LLM invocation

| Capability | Implementations | Verdict |
|---|---|---|
| Call an LLM | `TextEngine.stream_messages` (streaming) · `TextEngine.generate_response` (one-shot) | `single` — everything routes here. |
| Run a tool-calling loop | `tools/tool_loop.ToolLoop` · Claude Code's own loop (`cc-*` provider, DERPR's tools ignored by design) | `by design` — documented in `architecture.md`; `cc` is a clamped text provider, not a peer loop. |
| Get structured output without the tool loop | `sqlite_consolidator` (`submit_memory_summary`) · `ManagrAgent` second planner call (`submit_proposals`) · `DateTagger` / `ContentClassifier` inference agents | `by design` — "consolidator pattern", decision `2026-07-22-single-shot-inference-agents`. |
| Build the message array for a provider | `request_builder.format_raw_history_for_llm` · `stream_engine._build_messages` · `engine/driver._messages_to_history_object` · `utils/history_shape.extract_system_prompt` (moved down to the utils leaf by DP-317 — was `engine/providers/_shared`) · `providers/google.build_google_history` | `unreviewed` — **five**. `arch_audit similar` pairs three of them at 0.56-0.67. Provider-shape divergence is real, but the *history* half is not provider-specific. |
| Build the request **params** for a provider | `providers/openai.build_openai_params` · `providers/anthropic.build_anthropic_params` · `providers/google` inline config · `providers/local` / `_subprocess` (CLI argv instead of params) | `unreviewed` — noticed 2026-08-06, never ruled on. `arch_audit similar` scores the openai/anthropic pair at **0.62** (shared: `DEFAULT_TOKEN_LIMIT`, `api_params`, `config`, `current_message`). Distinct from the row above: that one is about *history shape*, this one about temperature/token/tool-schema plumbing. Plausibly justified by DP-244's "each provider owns its own message-build / param-build", but that was a package-layout decision, not a ruling on this duplication — so it needs either a decision record or a shared param-core. |
| Speak the inline `<tool_call>` text protocol | `src/text_tool_protocol.py` | `single` — DP-200 slice C unified agy/cc/stream_engine. **This is the model the rows above should follow.** |

## External processes and hosts

| Capability | Implementations | Verdict |
|---|---|---|
| Spawn a `claude` CLI subprocess | `engine/providers/cc.py` (as an LLM provider) · `self_edit/dispatcher.py` (as a supervised coding agent) | `by design` — different lifetimes (request-scoped vs detached+resumable). The sandbox-settings divergence recorded here before DP-314 is **resolved**: both now delegate to `utils/cc_sandbox.build_sandbox_settings()` and contribute only their own deltas (bridge host, notes path). `tests/utils/test_cc_sandbox.py` pins the parity and fails if they drift again. |
| Build the `claude --settings` sandbox block | `utils/cc_sandbox.build_sandbox_settings` | `single` — unified in DP-314; was two independent copies at 0.68 similarity. |
| Clone a git repo for an agent to work in | `self_edit/clone_manager` (derpr's own source: pristine base + per-dispatch worktree) · `utils/notes_workspace` (the notes repo: ONE shared clone that advances) | `by design` — opposite mutability on purpose. The base clone is never advanced because worktrees hang off it; the notes clone IS the working copy every agent edits. Shared git plumbing lives in `utils/git_support`. |
| Run a command on a remote host | `proxmox/ssh.SSHRunner` (argv-list, never a shell string) | `single` |
| Resolve a human-supplied name to an infra target | `proxmox/handler._resolve_guest` (guest hostname → `vmid`+`kind`, from live `pct list`/`qm list` state, exact + case-insensitive, refuses ambiguity) · `proxmox/handler._set_active_model` (friendly model name → systemd unit, from the units discovered per call by `_discover_units`) · `system_personas.model_selector` (LLM model name → catalog entry, **fuzzy**, via an LLM) | `by design` — DP-327. Three different namespaces with three different truth sources and three different failure policies, not three ways to do one thing. Guests come from **live node state** and change under you, so the map is rebuilt per call and a near-miss is *refused* with the list of real names — fuzzy matching a hostname is how you power off the wrong box. Model units used to come from a pinned config map; **DP-332 moved them onto the same live-state footing as guests** after the map drifted out of sync with the box in both directions (DP-329) — so both now share the rule that an unknown key is simply unknown, checked against what exists *now*. Only the LLM-catalog lookup is fuzzy, and it is deliberately an LLM call because the input is a human typing "flash" at `set model` and the worst case is a wrong completion, not a wrong power-off. ⚠️ A fourth resolver must state which of these three failure policies it takes before it is written; the tempting move — reusing the fuzzy one because it is friendliest — is the one that is wrong for anything destructive. |
| Enumerate what an infra host actually contains | `proxmox/handler._guest_inventory` (guests, from `pct list`/`qm list`) · `proxmox/handler._discover_units` (koboldcpp model units, from `systemctl list-unit-files`) · `proxmox/handler._gpu_status` (DRM cards, from `ls /sys/class/drm`) | `by design` — DP-327/DP-332. Three inventories over three unrelated namespaces on the same box; the shared rule, not the shared code, is the point: **the host is the authority and config must never assert what it contains.** Every one of these replaced or forestalled a hardcoded list, and the map they replaced (`PVE_MODEL_UNITS`) drifted silently in *both* directions — a stale entry was quietly omitted, and a unit the map had never heard of was never disabled, so it kept `:5001` and every swap failed to bind with nothing erroring (DP-329). ⚠️ A fourth "what does the box have" answer that reads from config instead of from the box is a bug being written, not a shortcut. Anything derived from a discovered listing must also be **anchored by an explicit pattern in Python** — `_UNIT_RE`, `_CARD_RE` — never by a glob sent to the remote shell: `ssh._reject_bad_args` forbids `*`, and the pattern is what stops a discovered set from including something it must not act on. |
| Parse a fixed-width CLI table into records | `proxmox/handler._parse_table` (header-offset slicing; `pct list` / `qm list`) | `single` — DP-327. Header-driven rather than whitespace-split **on purpose**: `pct list`'s `Lock` column is usually blank, so splitting on whitespace silently shifts `Name` into `Lock`'s slot and every guest gets the wrong name. Any second consumer of Proxmox CLI text must reuse this rather than re-derive a split. |
| Expose derpr tools to an external caller | `tools/mcp_bridge` (derpr as MCP **server**) | `single` |
| Consume an external tool server | `tools/mcp_client` (derpr as MCP **client**) | `single` |

## Messaging out

| Capability | Implementations | Verdict |
|---|---|---|
| Notify a human | `clients/notification.NotificationRouter` + `Notifier` impls (`DiscordNotifier`, `DiscordChannelNotifier`, `ZammadNotifier`, `LogNotifier`, voice `WebAlarmNotifier`) | `single` — the router is the pattern to copy. |
| Talk to Discord | `interfaces/discord_bot.CustomDiscordBot` (the one real client) · `clients/notification.Discord*Notifier` (wraps it) · `voice/capture.DiscordVoiceCapture` (voice transport) · `self_edit/integration.DiscordThreadClient` (**a `Protocol`**, structurally satisfied by `CustomDiscordBot` — not an implementation) | `single` — one client, three typed views of it. Checked 2026-07-27. |
| Resolve who to notify | `date_tagger._resolve_recipient` · `reminder_agent._resolve_recipient` · `dispatch_agent._resolve_recipient` | **accidental** — three copies of one function, similarity 0.57-0.62, same package. Lift to `agents/base.py`. |

## HTTP surface

| Capability | Implementations | Verdict |
|---|---|---|
| Serve HTTP | `interfaces/kobold_engine_adapter` FastAPI app (owns the lifespan) · `interfaces/portal_render` · `voice/web.attach_web` (mounts onto the adapter app) · `mcp_bridge` (ASGI sub-app on the same) | `by design` — one app, three mounts. |
| Authenticate a caller | `DERPR_CONTROL_TOKEN` control-plane middleware · `BridgeTokenStore` per-dispatch bearer tokens | `by design` — two principals, two credentials, documented in `mcp_bridge`. |
| Surface backend liveness/processing state to the user | `derpr_ui` `state/useKoboldPerf` → `StatusLine` (polls `/api/extra/perf` for busy/idle + last-gen counts, and `/api/extra/prefill` for live ingestion) · `api/client.usingMock`/`store.offline` → TopBar chip (is the *adapter* answering at all) | `by design` (DP-311) — two different subjects: the adapter's reachability vs the inference backend behind it. The perf poll deliberately bypasses `liveOr` so a dropped poll cannot flip the session's mock/offline verdict. Note the engine exposes **no** `/health` route; liveness is inferred from real calls. |
| Read model-backend state KoboldCPP exposes only on stdout | `services/kcpp-progress` sidecar (tails the log, serves parsed integers) → adapter `GET /api/extra/prefill` | `single` — the **only** sanctioned way to get log-only backend facts. Adding a second log reader, or letting the engine SSH to the model host, re-opens the egress/credential line closed in `2026-07-09-tool-security-lines`. Extend the sidecar instead. ⚠️ It must never return raw log lines — the log carries generated text. |

---

## Maintaining this file

The failure mode is not that this file is wrong — it is that it goes stale and
nobody notices, exactly like the import-linter contracts did before DP-302.

- Adding a capability, a `ServiceIntegration`, a store, a loop, or a notifier →
  add or update a row **in the same commit**.
- `python scripts/arch_audit.py similar concepts` surfaces *function*-level
  regeneration mechanically. It cannot see subsystem-level parallelism — two
  subsystems on different primitives share no vocabulary, no shape and no field
  names. That is the gap this file exists to cover, and it can only be filled by
  a human or an agent reading intent.
- Every `unreviewed` row is a question, not an accusation. Resolving one means
  either consolidating, or writing the decision record that makes it `by design`.
